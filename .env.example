# Square optional — leave unset for demo/POC (Vercel preview). Do NOT set on the public demo preview.
# SQUARE_APPLICATION_ID=
# SQUARE_ACCESS_TOKEN=
# SQUARE_LOCATION_ID=
# SQUARE_WEBHOOK_SIGNATURE_KEY=
# SQUARE_ENVIRONMENT=sandbox
# SQUARE_WEBHOOK_NOTIFICATION_URL=
# NEXT_PUBLIC_SQUARE_APPLICATION_ID=

# Admin kitchen board (required to enable /admin). Fail-closed when unset.
# Do not invent a value in git — set only in Vercel/local env.
ADMIN_PASSWORD=
